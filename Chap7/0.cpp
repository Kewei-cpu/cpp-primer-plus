#include <iostream>
#include <string>
#include <array>

using namespace std;

struct boss
{
};

struct map
{
};

int main()
{
    // 2
    void igor();
    float tofu(int);
    double mpg(double, double);
    long summation(double* array, int length);
    double doctor(string*);
    void ofcource(boss);
    string plot(map*);
}
